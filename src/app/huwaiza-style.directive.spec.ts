import { HuwaizaStyleDirective } from './huwaiza-style.directive';

describe('HuwaizaStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HuwaizaStyleDirective();
    expect(directive).toBeTruthy();
  });
});
